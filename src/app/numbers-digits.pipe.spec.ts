import { NumbersDigitsPipe } from './numbers-digits.pipe';

describe('NumbersDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new NumbersDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
